{
  "name": "e-market-pro",        // Your project name
  "version": "1.0.0",            // Version of your app
  "private": true,               // Prevents accidental publishing to npm
  "scripts": {                   // Scripts to run your app
    "start": "react-scripts start",   // Runs dev server (localhost:3000)
    "build": "react-scripts build",   // Creates production build (in /build folder)
    "test": "react-scripts test",     // Runs tests
    "eject": "react-scripts eject"    // Exposes config (optional, advanced)
  },
  "dependencies": {              // Libraries your project uses
    "react": "^18.2.0",          // React library
    "react-dom": "^18.2.0",      // React DOM rendering
    "react-scripts": "5.0.1"     // CRA scripts and configs
  }
}
